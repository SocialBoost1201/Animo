services:
  postgres:
    image: postgres:16-alpine
    container_name: animo-postgres
    environment:
      POSTGRES_USER: animo
      POSTGRES_PASSWORD: animo
      POSTGRES_DB: animo
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
